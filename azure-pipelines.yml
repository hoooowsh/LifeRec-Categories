trigger:
  branches:
    include:
      - main

pool:
  name: Agent-Sheng

jobs:
- job: Build
  steps:
  - checkout: self
    persistCredentials: true

  - bash: |
      git push git@github.com:hoooowsh/LifeRec-Categories.git
    displayName: 'Copy to Github'
    condition: eq(variables['Build.SourceBranch'], 'refs/heads/main')
